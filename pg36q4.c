#include<stdio.h>
int main(){
    float x,y,r,phi;
    printf("enter x");
    scanf("%f",&x);
    printf("enter y");
    scanf("%f",&y);
    r=sqrt(x*x+y*y);
        //tan inverse kaise DALU?
    printf("c");
    return 0;
}